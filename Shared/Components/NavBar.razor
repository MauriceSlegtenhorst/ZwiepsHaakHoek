<nav class="@(_cssClass)">
    <a class="logo" href="@UrlService.CreateUrl("/")">Zwieps Haak Hoek</a>
    
    <div class="links">
        @foreach (NavLink navLink in _navLinks)
        {
            <a class="@(navLink.CssClass)" href="@navLink.Url">@navLink.DisplayText</a>
        }
    </div>
    
    <div class="language-selector-container">
        <LanguageSelector />
    </div>

    <i class="fa-solid fa-hamburger hamburger" @onclick="OnHamburgerClick"/>
</nav>